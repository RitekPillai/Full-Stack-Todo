import 'package:flutter/material.dart';

class Updatetodo extends StatelessWidget {
  const Updatetodo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Hero(tag: 'name', child: Text("taskname")),
    );
  }
}
